# O valor real de __PORT__ é substituído pelo entrypoint.sh a partir da
# variável de ambiente PORT, fornecida pelo Render em tempo de execução
# (nunca fixa: o Render escolhe a porta e a informa via $PORT).
Listen __PORT__
